name: Say hello

on: [push]

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Say hello
        run: echo "Hello from GitHub Actions!"
